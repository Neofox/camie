
export const initialState = {
    baseUrl: "/",
    location: "/",
    user: {}
};


export default function sheetsReducer(state = initialState, action) {
    switch (action.type) {

        default:
            return state;
    }
}
